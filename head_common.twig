<!-- Meta -->
<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<meta name="csrf_token" content="{{ csrf_token() }}" />
<meta name="token" content="{{ Request.input('token') }}" />
<meta name="date_format" content="{{ Config.get('settings.date_format') }}" />
<meta name="time_format" content="{{ Config.get('settings.time_format') }}" />
<meta name="default_country" content="{{ Config.get('settings.default_country') }}" />
<meta name="max_file_size" content="{{ max_file_size }}" />
<meta name="allowed_files" content="{{ allowed_files }}" />
<meta name="environment" content="{{ Config.get('app.env') }}" />

<!-- Icon -->
<link rel="icon" href="https://sessioncommander.com/logo.svg" type="image/svg+xml" />
<link rel="icon" href="https://sessioncommander.com/favicon.ico" type="image/x-icon" />
{% if Config.get('settings.favicon') is not empty %}
    <link rel="icon" href="{{ Config.get('settings.favicon') }}" sizes="32x32"
          {% if Config.get('settings.favicon_dark_mode') is not empty %}media="(prefers-color-scheme: light)"{% endif %} />
{% endif %}
{% if Config.get('settings.favicon_dark_mode') is not empty %}
    <link rel="icon" href="{{ Config.get('settings.favicon_dark_mode') }}" sizes="32x32" media="(prefers-color-scheme: dark)" />
{% endif %}

{% set lightThemeColour = Config.get('template.themes.light.scheme.--color-background-primary') %}
{% set darkThemeColour = Config.get('template.themes.dark.scheme.--color-background-primary') %}

<meta name="theme-color" content="{{ get_template_mode(false) is same as('sp-theme-dark') ? darkThemeColour : lightThemeColour }}"
      media="(prefers-color-scheme: light)">
<meta name="theme-color" content="{{ darkThemeColour }}" media="(prefers-color-scheme: dark)" />

<!-- CSS -->
{{ colourScheme|raw }}
<script src="{{ asset_rev('/resources/assets/general/js/preload.js') }}"></script>
<link href="{{ asset_rev('resources/assets/frontend/css/main.min.css') }}" rel="stylesheet" />
<link href="{{ asset_rev('resources/assets/libs/css/common.min.css') }}" rel="stylesheet" />

<!-- SessionCommander Website Styling - Exact Match -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">

<style>
:root {
  --font-inter: 'Inter', sans-serif;
  --font-playfair: 'Playfair Display', serif;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: var(--font-inter), system-ui, sans-serif;
  background: linear-gradient(135deg, rgba(88, 28, 135, 0.15) 0%, rgba(111, 34, 50, 0.1) 50%, rgba(17, 24, 39, 1) 100%) !important;
  background-attachment: fixed !important;
  background-color: transparent !important;
  color: #f3f4f6 !important;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  padding-top: 80px;
}

/* Override SupportPal's body background classes */
body.sp-bg-secondary,
body.sp-bg-primary {
  background: linear-gradient(135deg, rgba(88, 28, 135, 0.15) 0%, rgba(111, 34, 50, 0.1) 50%, rgba(17, 24, 39, 1) 100%) !important;
  background-color: transparent !important;
}

h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-playfair), Georgia, serif;
  font-weight: 600;
  color: white;
}

/* Header - Exact match to website */
#main-header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 50;
  background-color: transparent;
  border-bottom: 1px solid transparent;
  transition: all 0.3s ease;
  will-change: background-color, border-color;
}

#main-header.scrolled {
  background-color: rgba(3, 7, 18, 0.95);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border-bottom: 1px solid rgba(168, 85, 247, 0.2);
}

#main-header .header-container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 1rem;
}

@media (min-width: 640px) {
  #main-header .header-container {
    padding: 0 1.5rem;
  }
}

@media (min-width: 1024px) {
  #main-header .header-container {
    padding: 0 2rem;
  }
}

#main-header .header-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 80px;
}

#main-header .logo-link {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  text-decoration: none;
}

#main-header .logo-link:hover img {
  transform: scale(1.1);
  transition: transform 0.2s;
}

#main-header .logo-link img {
  width: 40px;
  height: 40px;
  transition: transform 0.2s;
}

#main-header .logo-text {
  font-size: 1.25rem;
  font-weight: 700;
  color: white;
}

#main-header .nav-desktop {
  display: none;
  align-items: center;
  gap: 2rem;
}

@media (min-width: 1024px) {
  #main-header .nav-desktop {
    display: flex;
  }
}

#main-header .nav-link {
  color: #d1d5db;
  text-decoration: none;
  transition: color 0.2s;
  position: relative;
}

#main-header .nav-link:hover {
  color: white;
}

#main-header .nav-link::after {
  content: '';
  position: absolute;
  bottom: -4px;
  left: 0;
  width: 0;
  height: 2px;
  background: #9333ea;
  transition: width 0.3s ease;
}

#main-header .nav-link:hover::after {
  width: 100%;
}

/* All nav links should have the same styling */
#main-header .nav-link {
  color: #d1d5db;
  text-decoration: none;
  transition: color 0.2s;
  position: relative;
}

#main-header .nav-link:hover {
  color: white;
}

#main-header .nav-link::after {
  content: '';
  position: absolute;
  bottom: -4px;
  left: 0;
  width: 0;
  height: 2px;
  background: #9333ea;
  transition: width 0.3s ease;
}

#main-header .nav-link:hover::after {
  width: 100%;
}

#main-header .btn-login {
  background: linear-gradient(135deg, #7e22ce 0%, #9333ea 50%, #a855f7 100%);
  color: white;
  padding: 0.5rem 1rem;
  border-radius: 0.5rem;
  font-weight: 500;
  font-size: 0.875rem;
  text-decoration: none;
  display: inline-block;
  transition: opacity 0.2s;
}

#main-header .btn-login:hover {
  opacity: 0.9;
  color: white;
}

#main-header .mobile-menu-btn {
  display: block;
  color: #d1d5db;
  background: none;
  border: none;
  cursor: pointer;
  padding: 0.5rem;
}

@media (min-width: 1024px) {
  #main-header .mobile-menu-btn {
    display: none;
  }
}

#main-header .mobile-menu-btn:hover {
  color: white;
}

/* Mobile Menu */
#mobile-menu {
  display: none;
  position: fixed;
  top: 80px;
  left: 0;
  right: 0;
  background-color: #030712;
  border-top: 1px solid rgba(168, 85, 247, 0.2);
  z-index: 49;
}

#mobile-menu.open {
  display: block;
}

#mobile-menu .menu-content {
  padding: 1.5rem 1rem;
}

#mobile-menu .mobile-nav-link {
  display: block;
  color: #d1d5db;
  text-decoration: none;
  padding: 0.5rem 0;
  transition: color 0.2s;
}

#mobile-menu .mobile-nav-link:hover {
  color: white;
}

#mobile-menu .mobile-nav-divider {
  margin-top: 1rem;
  padding-top: 1rem;
  border-top: 1px solid #1f2937;
}

#mobile-menu .mobile-nav-resource {
  display: block;
  color: #9ca3af;
  text-decoration: none;
  font-size: 0.875rem;
  padding: 0.5rem 0;
  transition: color 0.2s;
}

#mobile-menu .mobile-nav-resource:hover {
  color: #a855f7;
}

#mobile-menu .mobile-btn-login {
  display: block;
  margin-top: 1rem;
  padding: 0.5rem 1rem;
  background: linear-gradient(135deg, #7e22ce 0%, #9333ea 50%, #a855f7 100%);
  color: white;
  border-radius: 0.5rem;
  text-align: center;
  font-size: 0.875rem;
  font-weight: 500;
  text-decoration: none;
}

/* Main Content */
main {
  min-height: calc(100vh - 80px);
  background: transparent;
  position: relative;
  z-index: 1;
}

.content-wrapper {
  max-width: 1280px;
  margin: 0 auto;
  padding: 2rem 1rem;
  background: transparent;
  position: relative;
  z-index: 1;
}

@media (min-width: 640px) {
  .content-wrapper {
    padding: 2rem 1.5rem;
  }
}

@media (min-width: 1024px) {
  .content-wrapper {
    padding: 2rem;
  }
}

/* SupportPal Content Styling - Match website background */
.sp-content-inner {
  background-color: transparent;
  border: none;
  border-radius: 0;
  padding: 0;
  color: #f3f4f6;
}

/* All SupportPal content should blend with website */
.sp-content,
.sp-content-inner,
.sp-wrapper {
  background-color: transparent;
}

/* Ensure no white overlays from SupportPal defaults */
body > *:not(header):not(footer):not(main),
body > div:not(.content-wrapper) {
  background-color: transparent !important;
}

/* Override any SupportPal default white backgrounds */
.sp-bg-primary,
.sp-bg-secondary,
[class*="sp-bg-"] {
  background-color: transparent !important;
}

/* Ensure no overlay is blocking interactions */
body::before,
body::after,
html::before,
html::after {
  display: none !important;
  pointer-events: none !important;
  content: none !important;
}

/* Make sure main content is interactive */
main,
.content-wrapper,
.sp-content-inner,
.sp-wrapper {
  pointer-events: auto !important;
  position: relative !important;
  z-index: 1 !important;
  background-color: transparent !important;
}

/* Override any SupportPal wrapper that might have white background */
.sp-wrapper,
.sp-content-wrapper {
  background-color: transparent !important;
  background: transparent !important;
}

/* Old breadcrumb styling - now using page-header-breadcrumb */
.sp-breadcrumb {
  display: none;
}

h1 {
  font-size: 2.5rem;
  margin-bottom: 1.5rem;
  font-family: var(--font-playfair);
  font-weight: 600;
  color: white;
  line-height: 1.2;
}

h2 {
  font-size: 2rem;
  margin-bottom: 1rem;
  font-family: var(--font-playfair);
  font-weight: 600;
  color: white;
}

h3 {
  font-size: 1.5rem;
  margin-bottom: 0.75rem;
  font-family: var(--font-playfair);
  font-weight: 600;
  color: white;
}

/* Forms - Match website styling */
input[type="text"],
input[type="email"],
input[type="password"],
input[type="search"],
textarea,
select {
  background-color: rgba(31, 41, 55, 0.5);
  border: 1px solid rgba(168, 85, 247, 0.2);
  color: #f3f4f6;
  border-radius: 0.375rem;
  padding: 0.5rem 0.75rem;
  width: 100%;
  transition: all 0.2s;
}

input[type="text"]:focus,
input[type="email"]:focus,
input[type="password"]:focus,
input[type="search"]:focus,
textarea:focus,
select:focus {
  border-color: #9333ea;
  outline: none;
  box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);
  background-color: rgba(31, 41, 55, 0.8);
}

/* Buttons */
button[type="submit"],
.sp-button,
.sp-form-button button {
  background: linear-gradient(135deg, #7e22ce 0%, #9333ea 50%, #a855f7 100%);
  color: white;
  border: none;
  padding: 0.5rem 1.5rem;
  border-radius: 0.5rem;
  font-weight: 500;
  cursor: pointer;
  transition: opacity 0.2s;
  font-size: 0.875rem;
}

button[type="submit"]:hover,
.sp-button:hover,
.sp-form-button button:hover {
  opacity: 0.9;
}

/* Links */
a {
  color: #a855f7;
  text-decoration: none;
  transition: color 0.2s;
}

a:hover {
  color: #c084fc;
}

/* Alerts */
.sp-alert {
  padding: 1rem;
  border-radius: 0.5rem;
  margin-bottom: 1rem;
}

.sp-alert-success {
  background-color: rgba(34, 197, 94, 0.1);
  border: 1px solid rgba(34, 197, 94, 0.3);
  color: #86efac;
}

.sp-alert-error {
  background-color: rgba(239, 68, 68, 0.1);
  border: 1px solid rgba(239, 68, 68, 0.3);
  color: #fca5a5;
}

.sp-alert-warning {
  background-color: rgba(251, 191, 36, 0.1);
  border: 1px solid rgba(251, 191, 36, 0.3);
  color: #fde047;
}

/* Footer - Exact match to website */
footer {
  background-color: #111827;
  border-top: 1px solid rgba(168, 85, 247, 0.2);
  padding: 3rem 1rem;
}

@media (min-width: 640px) {
  footer {
    padding: 3rem 1.5rem;
  }
}

@media (min-width: 1024px) {
  footer {
    padding: 3rem 2rem;
  }
}

footer .footer-container {
  max-width: 1280px;
  margin: 0 auto;
}

footer .footer-grid {
  display: grid;
  grid-template-columns: repeat(1, minmax(0, 1fr));
  gap: 2rem;
  margin-bottom: 2rem;
}

@media (min-width: 768px) {
  footer .footer-grid {
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }
}

footer .footer-brand {
  grid-column: span 1;
}

@media (min-width: 768px) {
  footer .footer-brand {
    grid-column: span 2;
  }
}

footer .footer-logo-link {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  margin-bottom: 1rem;
  text-decoration: none;
}

footer .footer-logo-link img {
  width: 40px;
  height: 40px;
}

footer .footer-logo-text {
  font-size: 1.25rem;
  font-weight: 700;
  color: white;
}

footer .footer-description {
  color: #9ca3af;
  font-size: 0.875rem;
  max-width: 28rem;
}

footer .footer-section h3 {
  color: white;
  font-weight: 600;
  margin-bottom: 1rem;
  font-size: 1rem;
}

footer .footer-section ul {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

footer .footer-section a {
  color: #9ca3af;
  text-decoration: none;
  font-size: 0.875rem;
  transition: color 0.2s;
}

footer .footer-section a:hover {
  color: #a855f7;
}

footer .footer-bottom {
  margin-top: 2rem;
  padding-top: 2rem;
  border-top: 1px solid #1f2937;
  display: flex;
  flex-direction: column;
  gap: 1rem;
  align-items: center;
  justify-content: space-between;
}

@media (min-width: 768px) {
  footer .footer-bottom {
    flex-direction: row;
  }
}

footer .footer-copyright {
  color: #6b7280;
  font-size: 0.875rem;
}

footer .footer-links {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  justify-content: center;
  align-items: center;
}

footer .footer-links a {
  color: #6b7280;
  text-decoration: none;
  font-size: 0.875rem;
  transition: color 0.2s;
}

footer .footer-links a:hover {
  color: #a855f7;
}

/* Scrollbar */
::-webkit-scrollbar {
  width: 10px;
}

::-webkit-scrollbar-track {
  background: #111827;
}

::-webkit-scrollbar-thumb {
  background: #6b21a8;
  border-radius: 5px;
}

::-webkit-scrollbar-thumb:hover {
  background: #7e22ce;
}

/* Cards - Match website styling */
.sp-dashboard-section,
.sp-card {
  background-color: rgba(31, 41, 55, 0.5);
  border: 1px solid rgba(168, 85, 247, 0.2);
  border-radius: 0.5rem;
  padding: 1.5rem;
  transition: all 0.2s ease;
}

.sp-dashboard-section:hover,
.sp-card:hover {
  border-color: rgba(168, 85, 247, 0.5);
  transform: translateY(-2px);
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
}

/* Tables - Match website styling */
table {
  background-color: rgba(31, 41, 55, 0.5);
  border-color: rgba(168, 85, 247, 0.2);
  color: #f3f4f6;
  width: 100%;
  border-collapse: collapse;
  border-radius: 0.5rem;
  overflow: hidden;
}

table th {
  background-color: rgba(55, 65, 81, 0.5);
  color: #f3f4f6;
  padding: 0.75rem;
  text-align: left;
  border-bottom: 1px solid rgba(168, 85, 247, 0.2);
}

table td {
  border-color: rgba(168, 85, 247, 0.2);
  padding: 0.75rem;
  border-top: 1px solid rgba(168, 85, 247, 0.2);
}

/* Dropdown - Match website styling */
.sp-dropdown {
  background-color: rgba(17, 24, 39, 0.95);
  backdrop-filter: blur(12px);
  border: 1px solid rgba(168, 85, 247, 0.2);
  border-radius: 0.5rem;
  padding: 0.5rem 0;
  min-width: 200px;
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
}

.sp-dropdown a {
  color: #d1d5db;
  text-decoration: none;
  display: block;
  padding: 0.5rem 1rem;
  transition: all 0.2s;
}

.sp-dropdown a:hover {
  color: white;
  background-color: rgba(31, 41, 55, 0.5);
}

/* Search - Match website styling */
.sp-search {
  background-color: rgba(3, 7, 18, 0.95);
  backdrop-filter: blur(12px);
}

.sp-search input[type="search"] {
  background-color: rgba(31, 41, 55, 0.5);
  border: 1px solid rgba(168, 85, 247, 0.2);
  color: #f3f4f6;
  border-radius: 0.375rem;
}

.sp-search input[type="search"]:focus {
  border-color: #9333ea;
  outline: none;
  box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);
}

/* Avatar */
.sp-avatar {
  border: 2px solid #9333ea;
  border-radius: 50%;
}

/* Custom Icons - Replace FontAwesome with SVG icons matching website */
.icon-wrapper {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 3rem;
  height: 3rem;
  flex-shrink: 0;
}

.icon-wrapper svg {
  width: 100%;
  height: 100%;
  color: #a855f7;
}

.icon-wrapper.large {
  width: 5rem;
  height: 5rem;
}

.icon-wrapper.small {
  width: 1.5rem;
  height: 1.5rem;
}

/* Remove backgrounds from icon wrappers - icons should be standalone */
.icon-wrapper[style*="background"],
.icon-wrapper,
[style*="background"][style*="icon-wrapper"],
[style*="background: rgba(168, 85, 247, 0.1)"],
[style*="background: rgba(251, 191, 36, 0.1)"] {
  background: none !important;
}

/* Remove backgrounds from SVG icon containers */
[style*="background"][style*="icon"],
[style*="background"] svg {
  background: none !important;
}

/* Ensure SVG icons don't have background containers */
svg[style*="color"] {
  background: none !important;
}

/* Dashboard grid */
.dashboard-grid {
  display: grid;
  grid-template-columns: repeat(1, minmax(0, 1fr));
  gap: 1.5rem;
}

@media (min-width: 768px) {
  .dashboard-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
}

/* Dashboard cards hover effect */
.dashboard-card:hover,
a.dashboard-card:hover {
  border-color: rgba(168, 85, 247, 0.5) !important;
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(168, 85, 247, 0.2);
}

/* All card links should have hover effects */
a[style*="background: rgba(31, 41, 55, 0.5)"][style*="border: 1px solid rgba(168, 85, 247, 0.2)"]:hover,
a[style*="background: rgba(31, 41, 55, 0.5)"]:hover {
  border-color: rgba(168, 85, 247, 0.5) !important;
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(168, 85, 247, 0.2);
}

/* Department cards hover */
.sp-department:hover {
  border-color: rgba(168, 85, 247, 0.5) !important;
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(168, 85, 247, 0.2);
}

/* Page Header - Like Features page */
.page-header {
  position: relative;
  padding: 4rem 0 3rem;
  background: linear-gradient(135deg, rgba(88, 28, 135, 0.2) 0%, rgba(111, 34, 50, 0.1) 50%, rgba(17, 24, 39, 0.5) 100%);
  margin: -2rem -1rem 3rem -1rem;
  overflow: hidden;
}

.page-header::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%239C92AC' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
  opacity: 0.5;
  pointer-events: none;
}

.page-header-content {
  position: relative;
  z-index: 1;
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 1rem;
}

.page-header-breadcrumb {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-bottom: 1.5rem;
  font-size: 0.875rem;
  color: #9ca3af;
  flex-wrap: wrap;
}

.page-header-breadcrumb a {
  color: #9ca3af;
  text-decoration: none;
  transition: color 0.2s;
}

.page-header-breadcrumb a:hover {
  color: #a855f7;
}

.page-header-breadcrumb-separator {
  color: #6b7280;
}

.page-header-title {
  font-size: 2.5rem;
  font-weight: 600;
  color: white;
  margin: 0;
  font-family: var(--font-playfair);
  line-height: 1.2;
}

.page-header-title-gradient {
  background: linear-gradient(135deg, #a855f7 0%, #c084fc 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.page-header-description {
  font-size: 1.125rem;
  color: #d1d5db;
  margin-top: 1rem;
  max-width: 48rem;
  line-height: 1.6;
}

/* Hide FontAwesome icons, use custom SVG instead */
.sp-icon i.fas,
.sp-icon i.far,
.sp-icon i.fab {
  display: none;
}

/* Additional SupportPal component styling */
.sp-form-row {
  margin-bottom: 1.5rem;
}

.sp-form-label {
  color: #d1d5db;
  font-size: 0.875rem;
  font-weight: 500;
  margin-bottom: 0.5rem;
  display: block;
}

.sp-icon {
  color: #a855f7;
}

/* Links in content */
.sp-content-inner a,
a[href] {
  color: #a855f7;
  text-decoration: none;
  transition: all 0.2s;
}

.sp-content-inner a:hover,
a[href]:hover {
  color: #c084fc;
  text-decoration: underline;
}

/* Text colors */
.sp-text-primary {
  color: #f3f4f6 !important;
}

.sp-text-secondary {
  color: #d1d5db !important;
}

.sp-text-tertiary {
  color: #9ca3af !important;
}

/* Background colors */
.sp-bg-primary {
  background-color: #030712 !important;
}

.sp-bg-secondary {
  background-color: #030712 !important;
}

/* Article Details Styling */
.sp-article-details {
  list-style: none;
  padding: 0;
  margin: 0.75rem 0 0 0;
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  font-size: 0.75rem;
  color: #6b7280;
}

.sp-article-details li {
  display: flex;
  align-items: center;
  gap: 0.25rem;
}

.sp-article-details a {
  color: #a855f7;
  text-decoration: none;
}

.sp-article-details a:hover {
  color: #c084fc;
}

.sp-item-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: inline-flex;
  gap: 0.5rem;
  flex-wrap: wrap;
}

/* Ticket Grid Styling */
.sp-filter-results {
  background: rgba(31, 41, 55, 0.5);
  border: 1px solid rgba(168, 85, 247, 0.2);
  border-radius: 0.5rem;
  padding: 1.5rem;
  margin-bottom: 1.5rem;
}

.sp-table {
  display: table;
  width: 100%;
}

.sp-table-row {
  display: table-row;
}

.sp-table-row label {
  display: table-cell;
  padding: 0.5rem;
  color: #d1d5db;
  font-weight: 500;
  width: 30%;
}

.sp-table-row > div {
  display: table-cell;
  padding: 0.5rem;
}

/* Ticket Messages Styling */
.sp-message {
  background: rgba(31, 41, 55, 0.5);
  border: 1px solid rgba(168, 85, 247, 0.2);
  border-radius: 0.5rem;
  padding: 1.5rem;
  margin-bottom: 1rem;
}

.sp-message-header {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-bottom: 1rem;
  flex-wrap: wrap;
}

.sp-message-header .sp-name {
  font-weight: 600;
  color: white;
  font-size: 1rem;
}

.sp-message-header .sp-name.sp-text-red-600 {
  color: #fca5a5;
}

.sp-message-header .sp-name.sp-text-green-600 {
  color: #86efac;
}

.sp-message-text {
  color: #d1d5db;
  line-height: 1.6;
}

.sp-message-text p {
  margin-bottom: 1rem;
}

.sp-message-text p:last-child {
  margin-bottom: 0;
}

/* Attachments Styling */
.sp-attachments {
  list-style: none;
  padding: 0;
  margin: 1rem 0 0 0;
}

.sp-attachments li {
  background: rgba(17, 24, 39, 0.5);
  border: 1px solid rgba(168, 85, 247, 0.2);
  border-radius: 0.375rem;
  padding: 0.75rem;
  margin-bottom: 0.5rem;
}

.sp-attachment-name {
  color: white;
  font-weight: 500;
  font-size: 0.875rem;
}

/* Article Content Styling */
.sp-article {
  color: #d1d5db;
  line-height: 1.75;
  font-size: 1rem;
}

.sp-article h1,
.sp-article h2,
.sp-article h3,
.sp-article h4,
.sp-article h5,
.sp-article h6 {
  color: white;
  font-family: var(--font-playfair);
  margin-top: 2rem;
  margin-bottom: 1rem;
}

.sp-article h1 { font-size: 2rem; }
.sp-article h2 { font-size: 1.75rem; }
.sp-article h3 { font-size: 1.5rem; }
.sp-article h4 { font-size: 1.25rem; }

.sp-article p {
  margin-bottom: 1rem;
}

.sp-article ul,
.sp-article ol {
  margin-bottom: 1rem;
  padding-left: 2rem;
  color: #d1d5db;
}

.sp-article li {
  margin-bottom: 0.5rem;
}

.sp-article a {
  color: #a855f7;
  text-decoration: none;
}

.sp-article a:hover {
  color: #c084fc;
  text-decoration: underline;
}

.sp-article code {
  background: rgba(31, 41, 55, 0.8);
  border: 1px solid rgba(168, 85, 247, 0.2);
  border-radius: 0.25rem;
  padding: 0.125rem 0.375rem;
  font-size: 0.875em;
  color: #c084fc;
}

.sp-article pre {
  background: rgba(31, 41, 55, 0.8);
  border: 1px solid rgba(168, 85, 247, 0.2);
  border-radius: 0.5rem;
  padding: 1rem;
  overflow-x: auto;
  margin-bottom: 1rem;
}

.sp-article pre code {
  background: none;
  border: none;
  padding: 0;
  color: #d1d5db;
}

.sp-article blockquote {
  border-left: 4px solid #9333ea;
  padding-left: 1rem;
  margin: 1rem 0;
  color: #9ca3af;
  font-style: italic;
}

.sp-article img {
  max-width: 100%;
  height: auto;
  border-radius: 0.5rem;
  margin: 1rem 0;
}

.sp-article hr {
  border: none;
  border-top: 1px solid rgba(168, 85, 247, 0.2);
  margin: 2rem 0;
}

/* Sidebar Styling */
.sp-sidebar {
  background: transparent;
}

.sp-sidebar-box {
  background: rgba(31, 41, 55, 0.5);
  border: 1px solid rgba(168, 85, 247, 0.2);
  border-radius: 0.5rem;
  padding: 1.5rem;
  margin-bottom: 1.5rem;
}

.sp-sidebar-box h2,
.sp-sidebar-box h4 {
  color: white;
  font-family: var(--font-playfair);
  font-size: 1.25rem;
  font-weight: 600;
  margin-bottom: 1rem;
}

/* Collapsible Styling */
.sp-collapsible {
  display: flex;
  align-items: center;
  cursor: pointer;
  user-select: none;
}

.sp-chevron {
  margin-left: auto;
}

.sp-chevron i {
  color: #9ca3af;
  transition: transform 0.2s;
}

/* Pagination Styling */
.pagination {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 0.5rem;
  margin: 2rem 0;
  flex-wrap: wrap;
}

.pagination a,
.pagination span {
  display: inline-block;
  padding: 0.5rem 0.75rem;
  background: rgba(31, 41, 55, 0.5);
  border: 1px solid rgba(168, 85, 247, 0.2);
  border-radius: 0.375rem;
  color: #d1d5db;
  text-decoration: none;
  transition: all 0.2s;
  font-size: 0.875rem;
}

.pagination a:hover {
  border-color: rgba(168, 85, 247, 0.5);
  color: white;
}

.pagination .active {
  background: linear-gradient(135deg, #7e22ce 0%, #9333ea 50%, #a855f7 100%);
  border-color: transparent;
  color: white;
}

/* DataTables Styling */
.dataTables_wrapper {
  color: #d1d5db;
}

.dataTables_wrapper .dataTables_filter input {
  background: rgba(31, 41, 55, 0.5);
  border: 1px solid rgba(168, 85, 247, 0.2);
  border-radius: 0.375rem;
  padding: 0.5rem 0.75rem;
  color: #f3f4f6;
}

.dataTables_wrapper .dataTables_length select {
  background: rgba(31, 41, 55, 0.5);
  border: 1px solid rgba(168, 85, 247, 0.2);
  border-radius: 0.375rem;
  padding: 0.5rem;
  color: #f3f4f6;
}

.dataTables_wrapper .dataTables_paginate .paginate_button {
  background: rgba(31, 41, 55, 0.5);
  border: 1px solid rgba(168, 85, 247, 0.2);
  border-radius: 0.375rem;
  color: #d1d5db;
  padding: 0.5rem 0.75rem;
  margin: 0 0.25rem;
}

.dataTables_wrapper .dataTables_paginate .paginate_button:hover {
  background: rgba(168, 85, 247, 0.2);
  border-color: rgba(168, 85, 247, 0.5);
  color: white;
}

.dataTables_wrapper .dataTables_paginate .paginate_button.current {
  background: linear-gradient(135deg, #7e22ce 0%, #9333ea 50%, #a855f7 100%);
  border-color: transparent;
  color: white;
}

/* Button Styling - Match website */
.sp-button,
button[type="submit"],
input[type="submit"] {
  background: linear-gradient(135deg, #7e22ce 0%, #9333ea 50%, #a855f7 100%);
  color: white;
  border: none;
  border-radius: 0.5rem;
  padding: 0.75rem 1.5rem;
  font-weight: 500;
  font-size: 0.875rem;
  cursor: pointer;
  transition: all 0.2s;
  text-decoration: none;
  display: inline-block;
}

.sp-button:hover,
button[type="submit"]:hover,
input[type="submit"]:hover {
  opacity: 0.9;
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(168, 85, 247, 0.3);
}

.sp-button-sm {
  padding: 0.5rem 1rem;
  font-size: 0.75rem;
}

.sp-button-secondary {
  background: rgba(31, 41, 55, 0.5);
  border: 1px solid rgba(168, 85, 247, 0.2);
}

.sp-button-secondary:hover {
  border-color: rgba(168, 85, 247, 0.5);
  background: rgba(31, 41, 55, 0.8);
}

/* Alert Styling - Match website */
.sp-alert {
  padding: 1rem 1.5rem;
  border-radius: 0.5rem;
  margin-bottom: 1rem;
  border: 1px solid;
  display: flex;
  align-items: start;
  gap: 0.75rem;
}

.sp-alert-success {
  background: rgba(34, 197, 94, 0.1);
  border-color: rgba(34, 197, 94, 0.3);
  color: #86efac;
}

.sp-alert-error {
  background: rgba(239, 68, 68, 0.1);
  border-color: rgba(239, 68, 68, 0.3);
  color: #fca5a5;
}

.sp-alert-warning {
  background: rgba(251, 191, 36, 0.1);
  border-color: rgba(251, 191, 36, 0.3);
  color: #fde047;
}

.sp-alert-info {
  background: rgba(59, 130, 246, 0.1);
  border-color: rgba(59, 130, 246, 0.3);
  color: #93c5fd;
}

.sp-alert-content {
  flex: 1;
  line-height: 1.6;
}

/* Editor Styling */
.tox-tinymce {
  border: 1px solid rgba(168, 85, 247, 0.2) !important;
  border-radius: 0.5rem !important;
  background: rgba(31, 41, 55, 0.5) !important;
}

.tox .tox-edit-area__iframe {
  background: rgba(31, 41, 55, 0.5) !important;
}

.tox .tox-toolbar,
.tox .tox-toolbar__overflow,
.tox .tox-toolbar__primary {
  background: rgba(17, 24, 39, 0.5) !important;
  border-bottom: 1px solid rgba(168, 85, 247, 0.2) !important;
}

.tox .tox-tbtn {
  color: #d1d5db !important;
}

.tox .tox-tbtn:hover {
  background: rgba(168, 85, 247, 0.1) !important;
}

.tox .tox-tbtn--enabled {
  background: rgba(168, 85, 247, 0.2) !important;
  color: #a855f7 !important;
}

/* Select2 Styling */
.select2-container--default .select2-selection--single,
.select2-container--default .select2-selection--multiple {
  background: rgba(31, 41, 55, 0.5) !important;
  border: 1px solid rgba(168, 85, 247, 0.2) !important;
  border-radius: 0.375rem !important;
  color: #f3f4f6 !important;
}

.select2-container--default .select2-selection--single .select2-selection__rendered {
  color: #f3f4f6 !important;
}

.select2-container--default .select2-selection--single .select2-selection__arrow b {
  border-color: #9ca3af transparent transparent transparent !important;
}

.select2-dropdown {
  background: rgba(17, 24, 39, 0.95) !important;
  backdrop-filter: blur(12px);
  border: 1px solid rgba(168, 85, 247, 0.2) !important;
  border-radius: 0.5rem !important;
}

.select2-results__option {
  color: #d1d5db !important;
}

.select2-results__option--highlighted {
  background: rgba(168, 85, 247, 0.2) !important;
  color: #a855f7 !important;
}

/* Hide SupportPal branding */
*[class*="supportpal"],
*[id*="supportpal"],
*[class*="SupportPal"],
*[id*="SupportPal"],
*[class*="support-pal"] {
  display: none !important;
  visibility: hidden !important;
}

/* Hide footer text containing SupportPal */
footer:has-text("SupportPal"),
footer:has-text("Self-Hosted"),
*:contains("Self-Hosted Help Desk Software by SupportPal") {
  display: none !important;
  visibility: hidden !important;
}

/* Ensure hidden elements stay hidden */
.sp-hidden,
[class*="hidden"]:not([class*="hover"]):not([class*="focus"]),
[id*="js-message-box"].sp-hidden {
  display: none !important;
  visibility: hidden !important;
}

/* Print styles */
@media print {
  body {
    padding-top: 0;
  }
  
  #main-header {
    position: static;
  }
  
  footer {
    border-top: none;
  }
}
</style>
